[project]
name = "string-theory"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy<2",
    "jax",
    "jaxlib",
]

[tool.uv]
dev-dependencies = []

[tool.pytest.ini_options]
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')",
]
