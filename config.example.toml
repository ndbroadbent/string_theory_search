[paths]
# cyrus-ga pool file (JSONL, one polytope per line keyed by "name")
pool = "/data/cyrus/pools/h21_4-7.jsonl"

# cyrus-ga run directory (rounds.jsonl / candidates.jsonl event streams)
run_dir = "/data/cyrus/runs/landscape"

# SQLite database for the dashboard
database = "data/string_theory.db"
